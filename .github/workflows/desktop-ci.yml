name: Desktop CI

on:
  push:
    branches: [main]
    paths:
      - 'apps/desktop/**'
      - 'package.json'
  pull_request:
    branches: [main]
    paths:
      - 'apps/desktop/**'

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        # See ci.yml — rm both lockfile + node_modules + force optional deps.
        # `shell: bash` makes the Unix `rm` work on Windows runners too
        # (default shell there is pwsh, where `rm -f` collides with
        # Remove-Item's ambiguous -Filter/-Force parameters).
        shell: bash
        run: |
          rm -f apps/desktop/package-lock.json
          rm -rf apps/desktop/node_modules
          npm install --prefix apps/desktop --include=optional --no-audit --no-fund

      - name: Build desktop app
        shell: bash
        run: cd apps/desktop && npm run build

      - name: Run tests
        shell: bash
        run: cd apps/desktop && npm test
